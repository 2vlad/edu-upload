# OpenAI API Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional defaults for model selection
NEXT_PUBLIC_DEFAULT_AI_MODEL=chatgpt5
DEFAULT_MODEL_CHOICE=chatgpt5
# Map UI choice "ChatGPT 5" to concrete model id
OPENAI_MODEL_CHAT_GPT_5=gpt-5-latest
ANTHROPIC_MODEL_SONNET_4=claude-3-5-sonnet-20241022

# Client: per-request upload cap (MB) to split uploads
NEXT_PUBLIC_MAX_UPLOAD_MB=4.5

# Server: cap for object generation output tokens
# GPT‑5 обычно требует 6000–8000, 7000 — безопасно
OPENAI_MAX_OUTPUT_TOKENS=7000
